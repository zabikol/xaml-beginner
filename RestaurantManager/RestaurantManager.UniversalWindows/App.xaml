<Application x:Class="RestaurantManager.UniversalWindows.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:RestaurantManager.UniversalWindows"
             xmlns:models="using:RestaurantManager.Models"
             RequestedTheme="Dark">
    <Application.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ResourceDictionary/myDefaultButtonTheme.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!--  Set my rounded button style as implicit style for all buttons  -->
            <Style TargetType="Button" BasedOn="{StaticResource myDefaultButtonTheme}" />

            <Style TargetType="TextBlock">
                <Setter Property="FontSize" Value="32" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>